# Деплой VidCourse на Vercel

## Настройка для Vercel

### 1. Подготовка Google OAuth

Обновите redirect URI в Google Cloud Console:
- Добавьте: `https://vidcourse-lesson-manager.vercel.app/auth/callback`
- Или используйте ваш кастомный домен

### 2. Настройка переменных окружения в Vercel

В настройках проекта Vercel добавьте следующие переменные окружения:

```env
GOOGLE_CLIENT_ID=ваш_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=ваш_client_secret
GOOGLE_REDIRECT_URI=https://vidcourse-lesson-manager.vercel.app/auth/callback
FLASK_SECRET_KEY=сгенерируйте-случайную-строку-минимум-32-символа
FLASK_ENV=production
```

### 3. Деплой

#### Вариант 1: Через GitHub (рекомендуется)

1. Подключите репозиторий к Vercel
2. Vercel автоматически обнаружит `vercel.json`
3. Добавьте переменные окружения в настройках проекта
4. Деплой произойдет автоматически

#### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Production деплой
vercel --prod
```

### 4. Важные замечания для Vercel

⚠️ **Ограничения Vercel:**
- Файловая система доступна только для чтения (кроме `/tmp`)
- Сессии хранятся в памяти (перезапускаются при каждом запросе)
- `users.json` будет храниться в `/tmp` (временное хранилище)

💡 **Рекомендации для production:**
- Используйте базу данных (например, Vercel Postgres, MongoDB Atlas, или Supabase) для хранения пользователей
- Используйте Redis или Vercel KV для сессий
- Рассмотрите использование Vercel Blob Storage для файлов

### 5. Обновление кода для production

Для production рекомендуется:

1. **Использовать базу данных вместо JSON файла:**
   - Vercel Postgres (бесплатный тариф)
   - MongoDB Atlas (бесплатный тариф)
   - Supabase (бесплатный тариф)

2. **Использовать Redis для сессий:**
   - Vercel KV (Redis)
   - Upstash Redis

3. **Обновить redirect URI:**
   - В Google Cloud Console
   - В переменных окружения Vercel

### 6. Проверка деплоя

После деплоя проверьте:
1. Откройте https://vidcourse-lesson-manager.vercel.app
2. Проверьте страницу входа
3. Попробуйте авторизоваться через Google
4. Проверьте работу API endpoints

### Troubleshooting

**Ошибка "Module not found":**
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте, что `api/index.py` правильно импортирует модули

**Ошибка "Session not working":**
- Vercel использует serverless функции, сессии могут не работать как ожидается
- Рассмотрите использование внешнего хранилища сессий

**Ошибка "File not writable":**
- Используйте `/tmp` для временных файлов
- Или используйте внешнее хранилище (S3, Vercel Blob)

**Ошибка OAuth redirect_uri_mismatch:**
- Убедитесь, что redirect URI в Vercel переменных точно совпадает с Google Cloud Console
- Проверьте, что используется HTTPS